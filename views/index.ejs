<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Welcome to <%= title %></h1>
    <% for(var i=0; i<messages.length; i++) { %>
      <div>
        <p><%=format(messages[i].added, {addSuffix: true})%></p>
        <h2><%= messages[i].user %></h2>
        <p><%= messages[i].text %></p>
      </div>
   <% } %>
   <a href="/new">Add New message</a>
  </body>
</html>
